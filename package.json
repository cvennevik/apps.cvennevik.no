{
  "packageManager": "pnpm@8.6.3",
  "scripts": { 
    "build": "pnpm -r build && node combine-build-outputs.mjs",
    "preview": "sirv ./dist public --cors --single --no-clear --port 8000"
  },
  "devDependencies": {
    "sirv-cli": "2.0.2"
  }
}
