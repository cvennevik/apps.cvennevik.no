{
  "packageManager": "pnpm@8.6.3",
  "scripts": { 
    "start": "sirv ./dist public --cors --single --no-clear --port 8000",
    "build-and-combine": "pnpm -r build && node combine-build-outputs.mjs"
  },
  "devDependencies": {
    "sirv-cli": "2.0.2"
  }
}
